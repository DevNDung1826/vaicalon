# 🎨 Công cụ Di chuyển Chi tiết Ảnh

Một công cụ web tương tác cho phép bạn chọn và di chuyển các phần nhỏ trong ảnh một cách dễ dàng.

## ✨ Tính năng

- 📁 **Tải ảnh lên**: Hỗ trợ các định dạng ảnh phổ biến (PNG, JPG, JPEG, GIF)
- ✂️ **Chọn vùng**: Vẽ hình chữ nhật để chọn bất kỳ phần nào của ảnh
- 🖱️ **Kéo thả**: Di chuyển các vùng đã chọn đến bất kỳ vị trí nào
- 🔄 **Xoay**: Xoay các phần tử đã chọn
- 🔍 **Phóng to/thu nhỏ**: Điều chỉnh kích thước hiển thị
- 🗑️ **Xóa**: Xóa các phần không mong muốn
- 💾 **Lưu**: Tải xuống ảnh đã chỉnh sửa

## 🚀 Cách sử dụng

1. **Mở file `index.html`** trong trình duyệt web
2. **Tải ảnh lên** bằng nút "Tải ảnh lên"
3. **Kích hoạt chế độ chọn vùng** bằng nút "Chế độ chọn vùng"
4. **Vẽ hình chữ nhật** trên ảnh để chọn vùng muốn di chuyển
5. **Kéo thả** vùng đã chọn để di chuyển
6. **Lưu ảnh** khi hoàn thành

## ⌨️ Phím tắt

- `Delete`: Xóa vùng đang chọn
- `Escape`: Hủy chọn / Thoát chế độ chọn
- `Ctrl + S`: Lưu ảnh

## 🎯 Hướng dẫn chi tiết

### Chọn và di chuyển phần tử:

1. Click nút "Chế độ chọn vùng" (nút sẽ chuyển sang màu tím)
2. Nhấn giữ chuột và kéo trên ảnh để tạo vùng chọn
3. Thả chuột để hoàn thành việc chọn
4. Click vào vùng đã chọn và kéo để di chuyển
5. Lặp lại để tạo nhiều vùng có thể di chuyển

### Xoay phần tử:

1. Chọn một vùng (click vào nó)
2. Dùng nút "↺ -15°" hoặc "↻ +15°" để xoay

### Xóa phần tử:

1. Chọn vùng muốn xóa
2. Click nút "Xóa vùng đã chọn" hoặc nhấn phím `Delete`

## 🛠️ Công nghệ sử dụng

- HTML5 Canvas
- Vanilla JavaScript (không cần thư viện bên ngoài)
- CSS3 với gradient hiện đại

## 📝 Lưu ý

- Công cụ hoạt động hoàn toàn trên trình duyệt, không cần kết nối internet
- Ảnh của bạn không được tải lên server nào
- Tất cả xử lý được thực hiện cục bộ trên máy tính của bạn

## 🌟 Ví dụ sử dụng

Công cụ này rất hữu ích cho:
- Chỉnh sửa sprite sheet
- Sắp xếp lại các phần tử trong ảnh
- Tạo composition ảnh mới
- Thiết kế nhân vật hoặc đồ họa

## 📧 Hỗ trợ

Nếu gặp vấn đề, vui lòng:
1. Đảm bảo bạn đang sử dụng trình duyệt hiện đại (Chrome, Firefox, Safari, Edge)
2. Kiểm tra console của trình duyệt (F12) để xem lỗi
3. Thử tải lại trang (F5)

---

Chúc bạn sử dụng vui vẻ! 🎉
